var mobile = require('./example');
var config = require('./dataconfig');

module.exports = {
	confData:config.confData,
	launchapp:mobile.launchapp,
	closeapp:mobile.closeapp,
	login:mobile.login,
	
}
